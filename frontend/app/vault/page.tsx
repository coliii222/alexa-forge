'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {api} from '../../lib/api';
export default function Vault(){const [keys,setKeys]=useState<any[]>([]); const [label,setLabel]=useState('Fake Key'); const [secret,setSecret]=useState('sk-test'); async function load(){setKeys(await api('/v1/vault/keys'))} useEffect(()=>{load()},[]); async function add(){await api('/v1/vault/keys',{method:'POST',body:JSON.stringify({provider:'fake',label,secret,priority:10})}); setSecret(''); load()} return <main style={{maxWidth:980,margin:'0 auto',padding:32}}><Link href="/">← Home</Link><h1>API Vault</h1><div className="card grid"><input className="input" value={label} onChange={e=>setLabel(e.target.value)} placeholder="Label"/><input className="input" value={secret} onChange={e=>setSecret(e.target.value)} placeholder="API secret"/><button className="btn" onClick={add}>Add Fake Key</button></div><div className="grid" style={{marginTop:16}}>{keys.map(k=><div className="card" key={k.id}><b>{k.label}</b> <span className="badge">{k.provider}</span><p>{k.secret_preview} | priority {k.priority} | active {String(k.is_active)}</p></div>)}</div></main>}
