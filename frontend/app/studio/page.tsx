'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {api} from '../../lib/api';
export default function Studio(){const [presets,setPresets]=useState<any[]>([]); const [prompt,setPrompt]=useState('Make this subject dance naturally.'); const [image,setImage]=useState('https://example.com/photo.jpg'); const [preset,setPreset]=useState(''); const [task,setTask]=useState<any>(null); const [err,setErr]=useState(''); useEffect(()=>{api<any[]>('/v1/presets').then(p=>{setPresets(p); setPreset(p[0]?.id||'')})},[]); async function submit(){setErr(''); const chosen=presets.find(p=>p.id===preset); const finalPrompt=(chosen?.prompt?chosen.prompt+'\n':'')+prompt; try{setTask(await api('/v1/generate/video',{method:'POST',body:JSON.stringify({mode:'image_to_video',prompt:finalPrompt,image_url:image,provider:'fake'})}))}catch(e:any){setErr(e.message)}} return <main style={{maxWidth:980,margin:'0 auto',padding:32}}><Link href="/">← Home</Link><h1>Motion Studio</h1><div className="card grid"><label>Image URL<input className="input" value={image} onChange={e=>setImage(e.target.value)}/></label><label>Preset<select className="input" value={preset} onChange={e=>setPreset(e.target.value)}>{presets.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Prompt<textarea className="input" rows={5} value={prompt} onChange={e=>setPrompt(e.target.value)}/></label><button className="btn" onClick={submit}>Generate Video</button>{err&&<pre style={{color:'#ff8b8b'}}>{err}</pre>}{task&&<div className="card"><b>Task #{task.id}</b> <span className="badge">{task.status}</span><p>Provider: {task.provider}</p>{task.output_url&&<p>Output: <a href={task.output_url}>{task.output_url}</a></p>}</div>}</div></main>}
