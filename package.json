{
  "scripts": {
    "dev:backend": "PYTHONPATH=backend backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "test:backend": "PYTHONPATH=backend backend/.venv/bin/pytest backend/tests/test_mvp.py -q"
  }
}
